name: Update Visitor Counter

on:
  schedule:
    - cron: "0 * * * *"  # Обновлять каждый час
  workflow_dispatch:

jobs:
  update_counter:
    runs-on: ubuntu-latest
    steps:
      - name: Update README
        uses: alexandresanlim/github-visitors-badge@main
        with:
          REPO: whxtelyy/whxtelyy
          USERNAME: whxtelyy